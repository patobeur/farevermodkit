# Prototypes natifs archivés

Ce dossier conserve des implémentations exploratoires qui ne participent plus à la compilation.

- `collection_atlas.*` : première surface Atlas, remplacée par `native/atlas_ui.*`.

Ces fichiers restent disponibles pour l’historique et la comparaison, mais ne doivent pas être réintroduits dans `native/build.cmd` sans revue.